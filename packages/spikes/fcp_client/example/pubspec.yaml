# Copyright 2025 The Flutter Authors.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

name: fcp_client_example
description: An example app for the fcp_client package.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fcp_client:
    path: ../
  cupertino_icons: ^1.0.8
  flutter_driver:
    sdk: flutter
  json_schema_builder:
    path: ../../../json_schema_builder

dev_dependencies:
  test: ^1.26.2
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
