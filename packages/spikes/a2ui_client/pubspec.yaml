# Copyright 2025 The Flutter Authors.
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

name: a2ui_client
description: A simplified client for the A2UI Streaming UI Protocol.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  a2a:
    # TODO: Update this to depend on the pub package after
    # https://github.com/shamblett/a2a/pull/39 is merged and published.
    git:
      url: https://github.com/jacobsimionato/shamblett-a2a.git
      ref: client-extensions
  equatable: ^2.0.7
  flutter:
    sdk: flutter
  freezed_annotation: ^3.1.0
  http: ^1.5.0
  json_annotation: ^4.9.0
  logging: ^1.3.0

dev_dependencies:
  build_runner: ^2.8.0
  dart_flutter_team_lints: ^3.5.2
  flutter_test:
    sdk: flutter
  freezed: ^3.2.3
  json_serializable: ^6.11.1
  mockito: ^5.5.1

flutter:
